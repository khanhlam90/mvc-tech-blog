<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title>
  <link rel="stylesheet" href="/stylesheets/style.css">
</head>

<body>
  <div class="wrapper">
      <header class="hero">
          <div>
            <nav class"nav">
                    <a class="main-link" href="/">Home</a>
                    <a class="main-link" href="/dashboard">Dashboard</a>
                    {{#if loggedIn}}
                    <a class="main-link" href="/" id="logout">Logout</a>
                    {{else}}
                    <a class="main-link" href="/login">Login</a>
                    {{/if}}
                </nav>          
          </div>
          <h1 class="app-title">The Tech Blog</h1>
      </header>
      <main>
        {{{ body }}}
      </main>
      <footer>
      Thanks for visiting!
    </footer>
  </div>

    {{#if loggedIn}}
    <script src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html>